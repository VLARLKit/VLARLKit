env_type: libero
task_suite_name: libero_10

total_num_envs: null

auto_reset: False
ignore_terminations: False
max_steps_per_rollout: 512
max_episode_steps: 512 # max episode steps for truncation
use_ordered_reset_state_ids: False

use_rel_reward: True
reward_coef: 5.0

# RLinf LiberoEnv specific settings
reset_gripper_open: True
is_eval: False

seed: 0
group_size: 1

video_cfg:
  save_video: False
  info_on_video: True
  video_base_dir: null

init_params:
  camera_heights: 256
  camera_widths: 256
